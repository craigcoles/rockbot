{
  "name": "rock-bot",
  "description": "The bot that everyone deserves, but not the one it needs right now.",
  "engines": {
    "node": "6.2.0"
  },
  "scripts": {
    "run": "set ENV=dev & node bot.js"
    "start": "set ENV=live & node bot.js"
  },
  "dependencies": {
    "botkit": "0.2.0",
    "dotenv": "^2.0.0"
  }
}
